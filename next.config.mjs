/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: ["images.unsplash.com", "imgs.search.brave.com","framerusercontent.com"],
  },
};

export default nextConfig;
